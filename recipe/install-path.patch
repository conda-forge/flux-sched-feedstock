From 11c1b85ac0f58009a4b7067f36547ea1679834b9 Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Jan=20Jan=C3=9Fen?= <janssen@mpie.de>
Date: Sun, 5 Oct 2025 22:41:51 +0200
Subject: [PATCH] fix make files

---
 CMakeLists.txt                         | 4 ++--
 resource/reapi/bindings/CMakeLists.txt | 2 +-
 2 files changed, 3 insertions(+), 3 deletions(-)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index 0998bb7..1ddffa2 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -146,8 +146,8 @@ message(STATUS "Boost version: ${Boost_VERSION}")
 # Install paths
 set(FLUX_CMD_DIR "${CMAKE_INSTALL_LIBEXECDIR}/flux/cmd")
 set(FLUX_LIB_DIR "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}/flux")
-set(FLUX_MOD_DIR "${FLUX_LIB_DIR}/modules")
-set(FLUX_SHELL_PLUGIN_DIR "${FLUX_LIB_DIR}/shell/plugins")
+set(FLUX_MOD_DIR "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}/flux/modules")
+set(FLUX_SHELL_PLUGIN_DIR "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}/flux/shell/plugins")
 set(FLUX_RC1_DIR "${CMAKE_INSTALL_SYSCONFDIR}/flux/rc1.d")
 set(FLUX_RC3_DIR "${CMAKE_INSTALL_SYSCONFDIR}/flux/rc3.d")
 
diff --git a/resource/reapi/bindings/CMakeLists.txt b/resource/reapi/bindings/CMakeLists.txt
index e64b2cf..befd53f 100644
--- a/resource/reapi/bindings/CMakeLists.txt
+++ b/resource/reapi/bindings/CMakeLists.txt
@@ -10,7 +10,7 @@ target_link_libraries( reapi_cli PRIVATE
     jobspec_conv
     flux::core
     )
-install(TARGETS reapi_cli LIBRARY DESTINATION "${FLUX_LIB_DIR}")
+install(TARGETS reapi_cli LIBRARY DESTINATION ${FLUX_LIB_DIR})
 
 add_library (reapi_module STATIC
     c++/reapi.hpp
